name: 'nyctlc_dbt'
version: '1.0'
config-version: 2
profile: 'nyctlc_profile'

model-paths: ['models']
seed-paths: ['seeds']
test-paths: ['tests']
analysis-paths: ['analysis']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

target-path: 'target'
clean-targets: ['target', 'dbt_packages']

models:
  nyctlc_dbt:
    +materialized: view
    staging:
      +schema: 'SILVER'   
    silver:
      +schema: 'SILVER'
    gold:
      +schema: 'GOLD'
